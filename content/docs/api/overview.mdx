---
title: "API 调用总览"
description: "ProAPI 的端点、认证、错误码与通用约定 —— 所有客户端集成的起点"
---

import { Callout } from 'fumadocs-ui/components/callout';
import { Card, Cards } from 'fumadocs-ui/components/card';
import { CpuIcon, BotIcon, SparklesIcon, FilmIcon } from 'lucide-react';

## 基本信息

| 项 | 值 |
| --- | --- |
| **API 域名** | `https://newapi.prorisehub.com` |
| **认证方式** | `Authorization: Bearer <你的令牌>` |
| **请求格式** | JSON（`Content-Type: application/json`），仅图片编辑接口走 `multipart/form-data` |
| **响应格式** | JSON；流式接口走 SSE (`text/event-stream`) |
| **协议** | HTTPS only |

<Callout type="info" title="先创建令牌">
  所有 API 调用都需要令牌。请先参照 [令牌与分组](/token/intro) 创建合适分组的令牌再回到本章节。**令牌的分组决定了你能调用哪些模型**。
</Callout>

## 选择哪个端点

ProAPI 同时兼容 **OpenAI / Anthropic / Google Gemini** 三套官方 API schema。根据你客户端期望的协议，选择对应章节：

<Cards>
  <Card
    href="/api/openai"
    icon={<CpuIcon />}
    title="OpenAI 兼容端点"
    description="/v1/chat/completions、/v1/images/generations 等，适配绝大多数 OpenAI SDK 与第三方客户端。"
  />
  <Card
    href="/api/anthropic"
    icon={<BotIcon />}
    title="Anthropic 兼容端点"
    description="/v1/messages 原生协议，Claude SDK / Claude Code / Cline 可直接接入。"
  />
  <Card
    href="/api/gemini"
    icon={<SparklesIcon />}
    title="Gemini 兼容端点"
    description="/v1beta/models/{model}:generateContent，Google 官方 SDK 协议。"
  />
  <Card
    href="/api/media"
    icon={<FilmIcon />}
    title="音视频模型参数映射"
    description="nano-banana / sora-2 / veo-3.1 / kling-3.0 等模型的尺寸、时长、比例参数如何映射到上游。"
  />
</Cards>

<Callout type="warn" title="同一个模型可以用多种端点吗">
  **可以**，但要选你客户端最适配的那一种。例如 Claude 模型既可以走 OpenAI 兼容的 `/v1/chat/completions`，也可以走原生的 `/v1/messages`。两条路都能用，但原生 `/v1/messages` 对 `tool_use`、`thinking`、`cache_control` 的支持更完整。
</Callout>

## 通用请求示例

```bash
curl https://newapi.prorisehub.com/v1/chat/completions \
  -H "Authorization: Bearer sk-你的令牌" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "claude-sonnet-4-6",
    "messages": [{"role": "user", "content": "你好"}],
    "stream": false
  }'
```

## 错误码

| HTTP 状态码 | 含义 | 排查建议 |
| --- | --- | --- |
| `401 Unauthorized` | 令牌错误 / 已禁用 | 检查 `Authorization` 头格式是否为 `Bearer sk-xxx`，并确认令牌未被删除或禁用 |
| `403 Forbidden` | 模型不在令牌分组内 | 在控制台「模型广场」确认当前分组是否包含目标模型，或更换分组令牌 |
| `400 model_price_error` | 模型未配置价格 | 联系管理员补充定价；通常出现在新接入但未上架的模型 |
| `429 Too Many Requests` | 触发分组限流 | 等待 1 分钟或单分钟内降低并发 |
| `500 / 502 / 503` | 上游错误 | 偶发可重试；持续出现请到 [check.linux.do/group/Pro](https://check.linux.do/group/Pro) 查看监控 |
| `504 Gateway Timeout` | 网关超时（Cloudflare 100s 限制） | 图片 / 视频生成耗时长，请参考 [音视频模型章节](/api/media) 的长连接说明 |

错误响应示例：

```json
{
  "error": {
    "message": "specific error description",
    "type": "new_api_error",
    "code": "model_price_error",
    "param": "",
    "request_id": "..."
  }
}
```

<Callout type="info" title="排障关键字段">
  `request_id` 是排障的关键。遇到无法解决的错误时，请将它与请求时间一起反馈给管理员。
</Callout>

## 流式响应（Stream）

聊天 / 对话类接口支持 `stream: true`，返回 Server-Sent Events 流：

```
data: {"id":"chatcmpl-...","object":"chat.completion.chunk","choices":[{"delta":{"content":"你"}}]}
data: {"id":"chatcmpl-...","object":"chat.completion.chunk","choices":[{"delta":{"content":"好"}}]}
data: [DONE]
```

<Callout type="warn" title="图像 / 视频不支持流式">
  请不要在 `/v1/images/*` 和音视频模型上设置 `stream: true`，会被上游拒绝。
</Callout>

## 速率与并发

- ProAPI 不设统一的全局限流，但**每个分组各自的上游会有限流**（如 Anthropic / Google 官方）。
- 单条请求最长执行时间受 Cloudflare 网关 **100 秒** 限制，触发后会返回 `504`。
- 视频生成本身耗时 3 ~ 6 分钟，**必须** 使用直连源站 + 本地客户端长连接策略，详见 [音视频模型章节](/api/media)。

## SDK 兼容性速查

| SDK / 客户端 | 默认走的端点 | 推荐 ProAPI 端点 | 注意 |
| --- | --- | --- | --- |
| OpenAI Python / Node | `/v1/chat/completions` | [`/v1/chat/completions`](/api/openai) | `base_url=https://newapi.prorisehub.com/v1` |
| Anthropic SDK | `/v1/messages` | [`/v1/messages`](/api/anthropic) | `base_url=https://newapi.prorisehub.com` |
| Google `@google/genai` | `/v1beta/models/...` | [`/v1beta/...`](/api/gemini) | `base_url=https://newapi.prorisehub.com` |
| Cherry Studio | OpenAI 兼容 | OpenAI 兼容端点 | API 地址填 `https://newapi.prorisehub.com` |
| Claude Code / Claude Desktop | Anthropic 原生 | [`/v1/messages`](/api/anthropic) | 参考 [CLI 配置](/cli/claude-code) |
| Cline / OpenCode / Cursor | OpenAI 兼容 | OpenAI 兼容端点 | 参考 [进阶用法](/advanced) |
